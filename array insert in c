#include <stdio.h>

int main()
{
    int a[10] = {1,2,3,4,5,6};
    int n = 6, pos = 3, value = 20, i;
    for(i = n; i >= pos; i--){
        a[i] = a[i - 1];}
    a[pos - 1] = value;
    for(i = 0; i <= n; i++){
        printf(" %d ", a[i]);}
    return 0;
}
